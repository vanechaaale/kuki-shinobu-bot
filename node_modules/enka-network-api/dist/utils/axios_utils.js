"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.fetchJSON = void 0;
const axios_1 = __importDefault(require("axios"));
/**
 * @param url
 * @param enka
 * @param enableTimeout
 */
async function fetchJSON(url, enka, enableTimeout = false) {
    const headers = { "User-Agent": enka.options.userAgent };
    if (enka.options.githubToken && url.startsWith("https://api.github.com/"))
        headers["Authorization"] = `Bearer ${enka.options.githubToken}`;
    const options = { headers };
    if (enableTimeout)
        options.timeout = enka.options.requestTimeout;
    const res = await (async () => {
        try {
            return await axios_1.default.get(url, options);
        }
        catch (e) {
            if (typeof e === "object" && e && "response" in e)
                return e.response;
            else
                throw e;
        }
    })();
    if (res.data) {
        try {
            res.data = JSON.parse(res.data);
        }
        catch (e) {
            // do not parse if it is not json due to some error
        }
    }
    return res;
}
exports.fetchJSON = fetchJSON;
