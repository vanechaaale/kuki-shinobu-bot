"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isJsonArray = exports.isJsonObject = exports.isJsonElement = void 0;
function isJsonElement(value) {
    const type = typeof value;
    if (type === "number" || type === "boolean" || type === "string" || value === null)
        return true;
    if (type !== "object" || value === undefined)
        return false;
    if (Array.isArray(value)) {
        return value.every(e => isJsonElement(e));
    }
    else {
        return Object.values(value).every(v => isJsonElement(v));
    }
}
exports.isJsonElement = isJsonElement;
function isJsonObject(value) {
    const type = typeof value;
    if (type !== "object" || value === null || value === undefined)
        return false;
    if (Array.isArray(value))
        return false;
    return Object.values(value).every(v => isJsonElement(v));
}
exports.isJsonObject = isJsonObject;
function isJsonArray(value) {
    const type = typeof value;
    if (type !== "object" || value === null || value === undefined)
        return false;
    if (!Array.isArray(value))
        return false;
    return value.every(e => isJsonElement(e));
}
exports.isJsonArray = isJsonArray;
